# InfinityX Desktop

Desktop build workspace for InfinityX Wallet.

```powershell
npm install
npm run exe:unpacked
```

The unpacked executable is written to `release/win-unpacked/InfinityX Wallet.exe`.
